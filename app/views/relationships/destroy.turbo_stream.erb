<% # When we follow => destroy action %>

<% # Replace the div with the id "unfollow_form" of the unfollow_form view
    # using the partial %>
<%= turbo_stream.update "follow_form" do %>
  <%= render partial: "users/follow" %>
<% end %>

<% # Replace the strong with the id "following" of the stats view
    # using the new count %>
<%= turbo_stream.update "followers" do %>
  <%= @user.followers.count %>
<% end %>